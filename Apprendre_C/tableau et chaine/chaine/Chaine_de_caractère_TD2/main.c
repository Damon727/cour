/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   main.c
 * Author: cmaillard
 *
 * Created on 1 décembre 2020, 13:08
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "fonctions.h"

int main(int argc, char** argv) {
    //------------------------------------EX 1(FACILE)-----------------------------
    /*
    char verbe[100];
    char aller[100] = {"aller"};

    printf("Donnez un verbe a l'infinitif \n");
    scanf("%s", verbe);


    if (verbe[strlen(verbe) - 1] == 'r' && verbe[strlen(verbe) - 2] == 'e' && strcmp(aller, verbe) != 0) {
        if (verbe[strlen(verbe) - 1] == 'r' && verbe[strlen(verbe) - 2] == 'e' && verbe[strlen(verbe) - 3] == 'g') {
            if (verbe[0] == 'a' || verbe[0] == 'e' || verbe[0] == 'i' || verbe[0] == 'o' || verbe[0] == 'u' || verbe[0] == 'y') {

                verbe[strlen(verbe) - 1] = ' ';
                printf("j' %s\n", verbe);

            } else {
                verbe[strlen(verbe) - 1] = ' ';
                printf("je %s\n", verbe);

            }
            verbe[strlen(verbe) - 1] = 's';
            printf("tu %s\n", verbe);
            verbe[strlen(verbe) - 1] = ' ';
            printf("il/elle %s\n", verbe);
            verbe[strlen(verbe) - 2] = 'e';
            verbe[strlen(verbe) - 1] = 'o';
            verbe[strlen(verbe)] = 'n';
            printf("nous %ss\n", verbe);
            verbe[strlen(verbe) - 3] = 'e';
            verbe[strlen(verbe) - 2] = 'z';
            verbe[strlen(verbe) - 1] = '\0';

            printf("vous %s\n", verbe);
            verbe[strlen(verbe) - 2] = 'e';
            verbe[strlen(verbe) - 1] = 'n';
            verbe[strlen(verbe) ] = 't';
            verbe[strlen(verbe) + 1] = '\0';
            printf("ils/elles %s\n", verbe);
        }
    else {
        if (verbe[0] == 'a' || verbe[0] == 'e' || verbe[0] == 'i' || verbe[0] == 'o' || verbe[0] == 'u' || verbe[0] == 'y') {

            verbe[strlen(verbe) - 1] = ' ';
            printf("j' %s\n", verbe);

        } else {
            verbe[strlen(verbe) - 1] = ' ';
            printf("je %s\n", verbe);

        }
        verbe[strlen(verbe) - 1] = 's';
        printf("tu %s\n", verbe);
        verbe[strlen(verbe) - 1] = ' ';
        printf("il/elle %s\n", verbe);
        verbe[strlen(verbe) - 2] = 'o';
        verbe[strlen(verbe) - 1] = 'n';
        verbe[strlen(verbe)] = 's';
        printf("nous %s\n", verbe);
        verbe[strlen(verbe) - 3] = 'e';
        verbe[strlen(verbe) - 2] = 'z';
        verbe[strlen(verbe) - 1] = '\0';

        printf("vous %s\n", verbe);
        verbe[strlen(verbe) - 2] = 'e';
        verbe[strlen(verbe) - 1] = 'n';
        verbe[strlen(verbe) ] = 't';
        verbe[strlen(verbe) + 1] = '\0';
        printf("ils/elles %s\n", verbe);
    }
    }
    */
//------------------------------------EX 2(Moyen)-----------------------------



    return (EXIT_SUCCESS);
}

